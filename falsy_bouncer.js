

function bouncer(arr) {
  // Don't show a false ID to this bouncer.
  return arr.filter(ans);
 
}
function ans(x){
  
  if (x !== false || x !== null || x !== 0 || x !== '' || x !== undefined || x !== NaN){
    return x;
  }
}

bouncer([7, 'ate', '', false, 9]);

